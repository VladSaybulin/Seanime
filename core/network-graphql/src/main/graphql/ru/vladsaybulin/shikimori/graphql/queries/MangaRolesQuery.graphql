query MangaRolesQuery($id: String!) {
    mangas(ids: $id) {
        personRoles {
            person { ...PersonFragment }
            rolesEn
        }
        characterRoles {
            character { ...CharacterFragment }
            rolesEn
        }
    }
}