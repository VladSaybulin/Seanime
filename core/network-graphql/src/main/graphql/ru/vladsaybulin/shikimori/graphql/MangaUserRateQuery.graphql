query MangaUserRateQuery($id: String!) {
    mangas(ids: $id) {
        userRate {
            id
            createdAt
            updatedAt
            status
            episodes
            rewatches
            score
            text
        }
    }
}
