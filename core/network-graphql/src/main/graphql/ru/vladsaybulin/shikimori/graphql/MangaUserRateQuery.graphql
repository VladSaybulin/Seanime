query MangaUserRateQuery($id: String!) {
    mangas(
        page: 1
        limit: 1
        ids: $id
    ) {
        userRate {
            id
            createdAt
            updatedAt
            status
            chapters
            volumes
            rewatches
            score
            text
        }
    }
}
