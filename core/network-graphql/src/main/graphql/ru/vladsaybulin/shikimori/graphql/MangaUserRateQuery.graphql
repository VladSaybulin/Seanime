query MangaUserRateQuery($id: String!) {
    mangas(ids: $id) {
        userRate {
            id
            createdAt
            updatedAt
            status
            chapters
            volumes
            rewatches
            score
            text
        }
    }
}
