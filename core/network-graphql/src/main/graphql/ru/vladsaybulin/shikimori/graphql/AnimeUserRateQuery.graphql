query AnimeUserRateQuery($id: String!) {
    animes(
        page: 1
        limit: 1
        ids: $id
    ) {
        userRate {
            id
            createdAt
            updatedAt
            status
            episodes
            rewatches
            score
            text
        }
    }
}
