query AnimeUserRateQuery($id: String!) {
    animes(ids: $id) {
        userRate {
            id
            status
            episodes
            rewatches
            score
            text
        }
    }
}
