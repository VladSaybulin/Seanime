query AnimeUserRateQuery(
    $ids: String!,
    $limit: PositiveInt!
) {
    animes(
        page: 1
        limit: $limit
        ids: $ids
    ) {
        id
        userRate {
            id
            createdAt
            updatedAt
            status
            episodes
            rewatches
            score
            text
        }
    }
}
