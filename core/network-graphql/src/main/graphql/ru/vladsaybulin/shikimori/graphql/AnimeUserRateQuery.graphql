query AnimeUserRateQuery($id: String!) {
    animes(ids: $id) {
        userRate {
            id
            createdAt
            updatedAt
            status
            episodes
            rewatches
            score
            text
        }
    }
}
